import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.transom.dipro',
  appName: 'Dipro',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
