import { MultiplierFormControlDirective } from './multiplier-form-control.directive';

describe('MultiplierFormControlDirective', () => {
  it('should create an instance', () => {
    const directive = new MultiplierFormControlDirective();
    expect(directive).toBeTruthy();
  });
});
